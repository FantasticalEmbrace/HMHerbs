# Copy to deploy/db-connection.env (gitignored) — used ONLY for import scripts.
# Do not commit real passwords.

DB_HOST=lin-xxxxx-xxxx.servers.linodedb.net
DB_PORT=3306
DB_USER=akmadmin
DB_PASSWORD=your_managed_db_password
DB_NAME=hmherbs
# Optional path to CA cert for mysql client
DB_SSL_CA=C:\path\to\ca-certificate.crt
