# HM Herbs Production Environment Configuration
# This file contains production-specific environment variables
# DO NOT commit this file with real values - use your deployment system to set these

# Database Configuration (Production)
DB_HOST=your_production_db_host
DB_USER=your_production_db_user
DB_PASSWORD=your_production_db_password
DB_NAME=hmherbs_production

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend Configuration (Production)
FRONTEND_URL=https://your-domain.com
PRODUCTION_DOMAIN=your-domain.com

# Security (CRITICAL: Generate strong, unique values)
JWT_SECRET=your_super_long_random_production_jwt_secret_here

# Redis Configuration (Production)
REDIS_URL=redis://your-redis-host:6379

# POS System Integration (Production)
POS_ENCRYPTION_KEY=your_production_pos_encryption_key

# Logging
LOG_LEVEL=warn

# Email Configuration (Production)
SMTP_HOST=your_production_smtp_host
SMTP_PORT=587
SMTP_USER=your_production_email
SMTP_PASS=your_production_email_password

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# SSL Configuration (if needed)
SSL_CERT_PATH=/path/to/ssl/cert.pem
SSL_KEY_PATH=/path/to/ssl/private.key

